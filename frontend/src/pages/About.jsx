function About({ settings }) {
	const salon = settings.data;
	const title = salon?.aboutTitle || "A little more care in every detail.";
	const description = salon?.aboutDescription || salon?.heroDescription || "A welcoming salon where skilled professionals create thoughtful, personalized beauty experiences with care, hygiene and quality products at every visit.";
	return <section className="section about-page"><div className="about-layout"><div className="about-media">{salon?.aboutImageUrl ? <img src={salon.aboutImageUrl} alt="About our salon" onError={(event) => { event.currentTarget.style.display = "none"; event.currentTarget.nextElementSibling.hidden = false; }} /> : null}<div className="image-fallback" hidden={Boolean(salon?.aboutImageUrl)}>About our salon</div></div><div className="about-copy"><span className="eyebrow">About {salon?.salonName || "our salon"}</span><h1>{title}</h1><p className="lead">{salon?.aboutShortDescription || "Where beauty, care and confidence come together."}</p><p>{description}</p>{salon?.mission && <div><h2>Our mission</h2><p>{salon.mission}</p></div>}{salon?.vision && <div><h2>Our vision</h2><p>{salon.vision}</p></div>}</div></div><div className="stats stats--inline"><div><strong>{salon?.yearsActive ?? "—"}</strong><span>years of craft</span></div><div><strong>{salon?.clientsServed ?? "—"}</strong><span>clients welcomed</span></div><div><strong>{salon?.staffCount ?? "—"}</strong><span>talents in the team</span></div></div></section>;
}

export default About;