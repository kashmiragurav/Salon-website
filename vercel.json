{
  "buildCommand": "npm install --prefix frontend && npm install --prefix admin-panel && npm run build --prefix frontend && npm run build --prefix admin-panel && rm -rf frontend/dist/admin && mkdir -p frontend/dist/admin && cp -R admin-panel/dist/. frontend/dist/admin/",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/admin/:path*",
      "destination": "/admin/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}